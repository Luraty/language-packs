# Copy to .env. Nothing in `make check`, `make catalog`, `make test`, `make de` or `make verify`
# needs any of this — the builders are dependency-free and read only local files. Publish path only.

# Hugging Face write token, read by hf/publish.py via HfApi().
# ⚠️ `make publish-de` is blocked by the licence gate regardless of this token, and stays blocked
# until somebody reads the Leipzig terms. A token does not open that gate.
HF_TOKEN=
